/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'assets.aceternity.com'], // Allow Unsplash domain for images
    },
    reactStrictMode: false,
  };
  
  export default nextConfig;
  